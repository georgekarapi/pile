{
  "name": "pileup",
  "private": true,
  "packageManager": "pnpm@12.4.2",
  "engines": { "node": ">=24 <25" },
  "workspaces": ["apps/*", "packages/*", "functions"],
  "scripts": {
    "build": "pnpm --filter @pileup/shared run build && pnpm --filter @pileup/functions run build",
    "dev": "pnpm --filter @pileup/mobile run start",
    "typecheck": "pnpm --filter @pileup/shared run build && pnpm --recursive --if-present run typecheck",
    "test": "pnpm --recursive --if-present run test",
    "lint": "pnpm --recursive --if-present run lint"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
